#include "main.h"

/**
 * int _strlen - returns the length of a string
 *
 * @s: string to mesure
 *
 * Return: lenght of the string
 */
int _strlen(char *s)
{
	int length = 0;

	for (length = 0; s[length] != '\0'; length++)
	{
	}
	return (length);
}
